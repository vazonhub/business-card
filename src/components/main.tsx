import { Header } from "./header"
import { Projects } from "./projects"

export const Main = () => {
    return (
        <section className='start'>
            <Header />
            <Projects />
        </section>
    )
}