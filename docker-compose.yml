version: '3.8'

services:
  frontend:
    image: vazon-business-card:latest
    build: .
    container_name: vazon-business-card
    ports:
      - "80:80"
      - "443:443"
    restart: unless-stopped